{
  "name": "nilicons",
  "version": "0.1.0",
  "description": "My personal icon set",
  "main": "index.js",
  "scripts": {
    "unzip": "unzip -o -d icons icons.zip && rm icons.zip",
    "svgo": "svgo --config=.svgo.yml -f icons -r",
    "icons": "npm run unzip && npm run svgo"
  },
  "author": "Nil Vila",
  "license": "MIT",
  "devDependencies": {
    "shelljs": "^0.8.3",
    "svgo": "^1.3.2"
  }
}
